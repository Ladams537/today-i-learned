In Go, `len s` gives you the number of **Bytes**, but `range s` gives you the **Runes** (characters). Range handles the weird UTF-8 stuff for us automatically. But when comparing anagrams length in the first place to see if valid, must use len(s) to keep granular version.

**Learning how to learn**